[package]
name = "x86_64-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
kernel-kit = { path = "../kernel-kit" }
kernel-orchestrator = { path = "../kernel-orchestrator" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
